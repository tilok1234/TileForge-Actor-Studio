{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base desktop capability for the Actor Studio window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
